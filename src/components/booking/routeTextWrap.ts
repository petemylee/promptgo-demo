/** Tailwind classes so long route / address text wraps fully instead of truncating with ellipsis. */
export const routeTextWrapClass =
  'min-w-0 break-words whitespace-normal [overflow-wrap:anywhere]';
